{
  "name": "agentkit",
  "version": "1.0.0",
  "private": true,
  "description": "AgentKit monorepo",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  },
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces",
    "clean": "npm run clean --workspaces",
    "lint": "npm run lint --workspaces"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  },
  "keywords": ["agent", "agentkit", "ai", "agents", "framework", "automation", "toolkit", "multi-agent"],
  "author": "Volodymyr Vreshch <vreshch@gmail.com>",
  "license": "MIT"
}
